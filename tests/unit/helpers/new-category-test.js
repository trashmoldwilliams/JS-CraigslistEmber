import { newCategory } from 'craigs-list/helpers/new-category';
import { module, test } from 'qunit';

module('Unit | Helper | new category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = newCategory([42]);
  assert.ok(result);
});
