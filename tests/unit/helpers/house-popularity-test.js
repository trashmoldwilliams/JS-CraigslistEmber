import { housePopularity } from 'craigs-list/helpers/house-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | house popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = housePopularity([42]);
  assert.ok(result);
});
